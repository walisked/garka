import React from 'react';
import { Box, Typography } from '@mui/material';

const Marketplace = () => (
	<Box sx={{ py: 6 }}>
		<Typography variant="h4" gutterBottom>
			Marketplace
		</Typography>
		<Typography color="text.secondary">Browse available properties (coming soon)</Typography>
	</Box>
);

export default Marketplace;
