import React from 'react';
import { Box, Typography } from '@mui/material';

const UserDashboard = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">User Dashboard (coming soon)</Typography>
  </Box>
);

export default UserDashboard;
