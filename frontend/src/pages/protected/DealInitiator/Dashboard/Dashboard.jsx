import React from 'react';
import { Box, Typography } from '@mui/material';

const DealInitiatorDashboard = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">Deal Initiator Dashboard (coming soon)</Typography>
  </Box>
);

export default DealInitiatorDashboard;
